// Synthetic fixture (not lifted from upstream): its directory layout
// (`foo/bar/mirror.proto`) mirrors its package (`foo.bar`), the standard
// protoc/Buf convention. This exists to pin the default-mode output-path
// doubling bug documented on outputPath in ../../../../generator.go: see
// that doc comment for the reproduction and the decision to replicate it
// byte-for-byte rather than fix it.
syntax = "proto3";

package foo.bar;

message Mirror {
  string name = 1;
}
